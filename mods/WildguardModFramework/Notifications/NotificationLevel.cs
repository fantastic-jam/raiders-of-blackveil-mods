namespace WildguardModFramework {
    /// <summary>Severity level for a WMF corner notification.</summary>
    public enum NotificationLevel {
        Info = 0,
        Warn = 1,
        Error = 2,
    }
}
